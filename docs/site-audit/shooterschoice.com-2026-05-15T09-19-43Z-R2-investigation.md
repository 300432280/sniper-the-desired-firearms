# R2 Live Investigation — shooterschoice.com

- **R1 candidate**: `docs/site-audit/shooterschoice.com-2026-05-15T08-53-56Z-R1.json`
- **R1 diff**: `docs/site-audit/shooterschoice.com-2026-05-15T08-53-56Z-R1-diff.md`
- **R2 corrections**: `docs/site-audit/shooterschoice.com-2026-05-15T09-19-43Z-R2-corrections.json`
- **Generated**: 2026-05-15T09:19:43Z
- **Audit IP**: residential Windows host; Cloudflare did not challenge.
- **Method**: live HTTP probes with curl + DB read + runtime-code review. No DB writes. 800ms inter-request delay observed in burst probes.

## R1's claims I trust the LEAST (and why)

R1 chose `wafType: cloudflare-passive` AND set `wafProbeEvidence.wordfenceInBody: false`, despite reporting that the XSS-shaped query returned a 403 with a 17925-byte body. R1 measured the size signature but did not READ the body. This is exactly the "static probe doesn't see what a body grep would" problem the project's WAF probe was rewritten to handle in 2026-04-15 (Mistake 36, bullseyenorth.com). I used the body grep technique R1 skipped.

R1 also chose `verifyMethod: store-api` on a WC site without testing what Store API does to OOS products. SKILL.md's derived-rules table says "WC default = store-api", but a derived rule isn't a verification — it's a starting hypothesis. I tested it live.

## Field-by-field investigation

### 1. wafType — R1 `cloudflare-passive`, DB `cloudflare-passive`, R2 says **wordfence-on-cloudflare-passive**

**R1's claim**: "Cloudflare WAF rule-selective on XSS payload only." `wafProbeEvidence.wordfenceInBody: false`.

**Live probe** (different method than R1 — body grep, not just status+size):

```
curl -sS -D - -o body.txt "https://shooterschoice.com/?s=%3Cscript%3Ealert(1)%3C/script%3E&post_type=product"
HTTP/1.1 403 Forbidden
Server: cloudflare
CF-RAY: 9fc10cac694a4a29-YYZ
(body 17925 bytes)

grep -ioE "wordfence|generated by wordfence|sucuri|cloudflare|attention required|cf-ray|malcare" body.txt | sort -u
> Generated by Wordfence
> Wordfence
> wordfence
```

**Verdict**: Wordfence body markers present in 3 distinct positions. Cloudflare is in front (CF-RAY on every response) but the WAF rule firing on the XSS payload is **Wordfence**, not Cloudflare managed rules.

R1's evidence and value disagree with each other — `xssRuleFired: true` while `wordfenceInBody: false` is impossible if the 403 body has "Generated by Wordfence" in it. R1 did not grep the body. The DB has the same misclassification.

Operational impact: minor. Normal crawler traffic passes through both layers (homepage 200, Store API 200). But the wafType field downstream may control which WAF-cookie strategy is chosen, and Cloudflare's Turnstile flow differs from Wordfence's challenge flow.

### 2. expectedProductCount — R1 `4493`, DB `11411`, R2 says **11370**

R1's value is the customer-visible in-stock subset. DB's value is the admin REST total but stale by ~33 days. Live ground truth = 11370.

**Three live REST probes**:

```
GET /wp-json/wc/store/v1/products?per_page=1
  -> x-wp-total: 4493  (Store API default = in-stock only)

GET /wp-json/wp/v2/product?per_page=1
  -> x-wp-total: 11370  (admin REST = all publish statuses)

GET /wp-json/wc/store/v1/products?per_page=1&stock_status=instock,outofstock,onbackorder
  -> x-wp-total: 11370  (Store API with all stock = same as admin)

GET /wp-json/wc/store/v1/products?per_page=1&stock_status=outofstock
  -> x-wp-total: 6877   (OOS-only subset)

Arithmetic: 4493 + 6877 = 11370 verified.
```

The "true" number depends on what you're counting. For the **watermark crawler** that walks after-date, the discovery endpoint must see ALL published products (in-stock + OOS, since stock state flips both ways). For **customer-visible HTML walks**, only 4493 matter.

### 3. productCountMethod.endpoint — R1 `Store API`, DB `admin REST`, R2 says **admin REST**

Follows from #2. The DB's choice (admin REST `/wp-json/wp/v2/product`) is the correct discovery endpoint. R1's Store API endpoint with default in-stock-only filter would silently drop 60% of the catalog.

### 4. catalogUrls — R1 `[/shop/]`, DB `30 /category/...`, R2 says **either works; recommend /shop/**

**Probed all 30 DB URLs**: every single one returns HTTP 200. None are 404.

**R1's claim** (in R1 diff): the typo-slugs `4027-accessoires` and `tbsarrows-componets` are typo-duplicates. WRONG. Live result-count check:

```
/category/4027-accessoires/ -> "Showing 1-40 of 112 results"  <title>ACCESSORIES Archives ...
/category/4027-accessories/ -> "Showing 1-40 of 1068 results" <title>ACCESSORIES Archives ...
/category/tbsarrows-componets/ -> "Showing 1-40 of 60 results"  <title>TBSArrows & Componets Archives ...
/category/tbsarrows-components/ -> separate count (194 per DB)
```

These are real distinct WP categories the merchant created with misspelled slugs, NOT URL typos. They contain 112 and 60 real products respectively. The merchant tagged products inconsistently.

**Sum check**: 30 DB URLs sum to 5542 vs `/shop/` visible 4493 -> 19% overlap from leaf-subcategories double-counted (e.g., `centerfire-rifle-ammo` is a subset of `4021-ammunition`).

**Coverage**: Both shapes cover 100% of the 4493 in-stock visible products. /shop/ is cleaner; DB shape gives operator more granularity.

### 5. crawlers.maintain.verifyMethod — R1 `store-api`, DB `json-ld`, R2 says **json-ld**

This is the most operationally consequential correction.

**Hypothesis**: WC Store API DEFAULTS to hiding out-of-stock products. If true, R1's `store-api` verify will silently fail any product that flipped to OOS.

**Test**: Find 3 OOS product IDs from Store API itself (`?stock_status=outofstock`):
```
ids 263618, 263616, 263612 — all is_in_stock:false
```

**Probe DEFAULT Store API include=**:
```
GET /wp-json/wc/store/v1/products?include=263618,263616,263612
-> []   (empty — DEFAULT hides OOS)

GET /wp-json/wc/store/v1/products?include=263618,263616,263612&stock_status=instock,outofstock
-> 3 items, all is_in_stock:false   (explicit filter restores them)
```

**Verdict**: R1's `store-api` would, in production, batch verify products and miss every OOS item — flagging them as "missing." The site's frontend keeps these products at HTTP 200, so json-ld verification works correctly.

**worker.ts:381-400 literal-equality verdict**: line 397 checks `maintainConfig.verifyMethod !== 'store-api'` exactly. With `json-ld` in profile, the Store API fast-path (lines 437-505) is SKIPPED -> caller falls through to whatever the adapter's verify method is (Playwright per-product or HTML json-ld parse). Line 453 — the actual Store API call — sends `params: { include: ids, per_page: CHUNK_SIZE }` with NO stock_status param, exactly the failure case I tested.

### 6. paginationPattern — runtime simulation (REQUIRED)

**catalog-crawler.ts:118-125** code path for `type: 'path'`:
```
if (pattern?.type === 'path') {
  const template = pattern.template || '/page/{N}';
  const stripped = baseUrl.endsWith('/') ? baseUrl.slice(0, -1) : baseUrl;
  return `${stripped}${template.replace('{N}', String(pageNum))}`;
}
```

**Simulation**:
- buildPaginatedUrl('https://shooterschoice.com/shop/', 2, { type:'path', template:'/page/{N}/' })
- stripped = 'https://shooterschoice.com/shop' (trailing slash removed)
- template.replace = '/page/2/'
- result = 'https://shooterschoice.com/shop/page/2/'

**Live HTTP probe of generated URLs**:
```
/shop/page/2/   -> HTTP 200, 1396740 bytes
/shop/page/3/   -> HTTP 200, 1399190 bytes
/shop/page/112/ -> HTTP 200, 1401422 bytes
/shop/page/113/ -> HTTP 200, 1328280 bytes  (last page, smaller)
```

Also tested against DB-shape baseUrl `/category/4022-firearms/`:
```
/category/4022-firearms/page/2/  -> HTTP 200, 904405 bytes
/category/4022-firearms/page/11/ -> HTTP 200, 902456 bytes
```

**PAGINATION TEMPLATE VERDICT**: CORRECT. The runtime concatenation logic produces valid URLs on both `/shop/` and `/category/` baseUrls. No 404s. No leading-slash-missing edge case (template starts with `/`, baseUrl trailing-slash is stripped). R1's pagination shape is fully verified.

## Cross-checks vs runtime code

- `product-count-probe.ts:149-154`: `wp-rest-header` method reads `x-wp-total` header. Verified live both endpoints return this header correctly.
- `catalog-crawler.ts:118-125`: pagination template handling verified above.
- `worker.ts:381-400`: literal-equality check confirmed; verifyMethod string must be exactly `'store-api'` to enable fast-path.
- `worker.ts:452-457`: Store API call uses default params with NO stock_status — confirms OOS-blindspot risk.
- `scraper/adapters/woocommerce.ts:242-274`: getNextPageUrl() and other navigation; not directly tested but unrelated to corrections.

## What I did NOT verify

- Whether `crawl-watermark`'s `api-date-since-watermark` walk against `/wp-json/wp/v2/product` actually surfaces drafts/private with valid `date` fields (we only verified `status:publish` items returned). Operator's bootstrap dataFlow assumes admin REST sees more than Store API — confirmed for OOS, not confirmed for drafts.
- Whether `wordfence-on-cloudflare-passive` is a valid `wafType` enum value in the project. The site profile likely needs a separate `wafLayers: ['cloudflare-passive', 'wordfence-rule-selective']` shape if the enum is constrained. I left the field as a single string for now; assembly-time validation should resolve.
- Behavior of R1's `/shop/` walk under rate-limiting from a non-residential IP. R1 noted this caveat already.

## Final note on R1 quality

R1 is solid on platform detection, sort verification, pagination template, ageGate, extraction, and most evidence collection. R1 had THREE specific failure modes that R2 corrected:

1. **Did not grep WAF 403 body** -> wrong wafType + contradictory wordfenceInBody.
2. **Trusted SKILL.md derived-rule table without testing the OOS edge case** -> wrong verifyMethod.
3. **Picked Store API as count source without realising its default filter hides 60% of catalog** -> wrong expectedProductCount + wrong productCountMethod.endpoint.

Of these, (2) is the highest-impact correction — would have caused silent data loss in production maintain cycles.
