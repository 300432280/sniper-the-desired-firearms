# Pre-Bootstrap R1 Diff — shooterschoice.com

**Candidate:** `docs/site-audit/shooterschoice.com-2026-05-13T08-24-32Z-R1.json`
**DB siteProfile:** read via Prisma `monitoredSite.findFirst({where:{domain:'shooterschoice.com'}})` at 2026-05-13T08:25Z.
**DB `lastVerified`:** 2026-04-12 (31 days stale).

---

## Divergent fields (12)

| # | Field | Candidate | DB | One-line WHY |
|---|---|---|---|---|
| 1 | `wafType` | `"wordfence"` | `"cloudflare-passive"` | DB classification looked at Cloudflare + XSS rule and called it "cloudflare-passive with active XSS rule" in notes; candidate identified that the 403 body explicitly contained Wordfence branding ("About Wordfence... installed on over 5 million WordPress sites... Generated by Wordfence at"), so the 403 source IS Wordfence plugin, not Cloudflare. Cloudflare is passive (cf-ray on every batch but no challenge); Wordfence is the operational WAF. |
| 2 | `expectedProductCount` | `4469` | `11411` | DB uses `wp/v2/product` (admin REST, includes drafts/private posts); candidate uses `wc/store/v1/products` (customer-visible, excludes drafts). Live `/shop/` HTML says "Showing 1-40 of 4469 results" - the 4,469 is the storefront truth; 11,411 still appears in candidate's `wp/v2/product` cross-check probe. Both numbers are real; the question is which one the consumer should treat as "expected catalog size". Candidate chose customer-visible per Stage 8 priority order. |
| 3 | `productCountMethod.endpoint` | `/wp-json/wc/store/v1/products` | `/wp-json/wp/v2/product` | Follows from #2 - candidate routed count probe through WC Store API to match the customer-visible total. |
| 4 | `productCountMethod` extra fields | (only `method`/`endpoint`/`header`) | adds `wpRestTotal: 11411`, `storeApiTotal: 4311`, `dateFilterEvidence: {...}`, `dateFilterVerified: true` | DB has operator audit-trail residue (per Rule B) baked into runtime field; candidate kept runtime fields only. Note: DB's `storeApiTotal: 4311` differs from today's live 4,469 (catalog grew by 158 in 31 days). |
| 5 | `catalogUrls` | `["/shop/"]` (1 URL) | 30 per-category URLs | Major divergence. DB enumerates every top-level WC category individually; candidate consolidated to `/shop/` because (a) `/shop/` returns the exact same count as global WC Store API (4,469 byte-for-byte match), proving 100% coverage, and (b) Rule C says "if one URL reaches 100%, use just that" + minimum URL count is the goal. DB's 30-URL approach is the per-category-spine shape; candidate is the single-spine shape. Both can reach 100% coverage but the candidate has not full-walked per-category to prove independent coverage matches union. Operator-judgment field. |
| 6 | `paginationPattern.template` | `/shop/page/{N}/` | `/page/{N}/` | DB stores a relative path suffix (appended to whatever catalogUrl is being walked - works for all 30 per-category URLs). Candidate baked the `/shop/` prefix into the template since only `/shop/` is the catalog URL. Both are runtime-valid; DB's is the more reusable shape. |
| 7 | `paginationPattern` extra fields | candidate adds `perPage: 40`, `firstPageHasParam: false`, `startPage: 1`, `zeroIndexed: false` | DB omits these | DB profile is older and used a leaner shape; candidate added the full v2 shape per Stage 5 spec. Runtime consumers may treat missing values as defaults (perPage=20, startPage=1, etc.) - DB's lean shape relies on defaults. |
| 8 | `crawlers.maintain.verifyMethod` | `"store-api"` | `"json-ld"` | Different verify strategies. DB uses JSON-LD scraped from product detail pages; candidate followed Stage 3's deterministic platform-to-verifyMethod table (`woocommerce` -> `store-api`). DB's choice predates that table or is a deliberate operator override. The Stage 3 table cites `worker.ts:tryStoreApiVerify` as the consumer - need to verify DB's `"json-ld"` value is also valid in that switch. |
| 9 | `crawlers.maintain.verifyEndpoint` | `/wp-json/wc/store/v1/products` | (absent) | Follows from #8. Candidate added the WC Store API endpoint; DB doesn't need it because `json-ld` reads HTML. |
| 10 | `crawlers.bootstrap.apiEndpoints` | `{wcStore, wpRest}` flat | `{priceEnrichment, productDiscovery}` semantic keys | Same two endpoints, different key naming. DB uses purpose-named keys (`priceEnrichment` for WC Store, `productDiscovery` for wp/v2/product); candidate used platform-named keys. DB's naming is more self-documenting; candidate's is more mechanical. |
| 11 | DB-only fields not produced | n/a | `budget: 90`, `timeout: 15000`, `t1IntervalMin: 17`, `crawlPhase: "bootstrap"`, `crawlers.maintain.cooldowns/tierShares/tierWindows`, `crawlers.bootstrap.method/htmlFallback`, `dataFlow`, `expectedInStockCount: 4311`, `hasRateLimit: false`, `searchUrl`, `sortVerifiedAt`, `siteCategory: "retailer"` | DB has runtime scheduler/budget config NOT produced by pre-bootstrap (operator-tunable post-bootstrap fields). Candidate per-spec only emits the field set in SKILL.md "Output target" - this is by design, not a gap. |
| 12 | `extractionTested` + `extractionSample` | candidate adds both | DB omits | Stage 4g additions; DB profile predates Stage 4g spec. Pure addition, no contradiction. |

---

## Non-divergent (agreement)

`platform`, `adapterType`, `hasWaf`, `hasCaptcha`, `needsPlaywright`, `sortParam` (`?orderby=date`), `sortVerified` (true), `perPage` (40), `paginationPattern.type` (path), `crawlers.watermark.method` (api-date-since-watermark), `profileVersion` (implicit), `ageGate.detected` (false implicit).

---

## Skill-judgment vs Operator-judgment classification

- **Skill-mechanically-correct, operator-may-override:** `catalogUrls` (1 vs 30), `wafType` (wordfence vs cloudflare-passive), `expectedProductCount` (4469 vs 11411), `productCountMethod.endpoint`
- **Skill-spec divergence from DB-legacy shape:** `paginationPattern.template` (full path vs suffix), `crawlers.bootstrap.apiEndpoints` keys, `productCountMethod` extra fields
- **Skill omits-by-design (not a gap):** `budget`, `timeout`, scheduler/tier config, `searchUrl`, `siteCategory`, `expectedInStockCount`, `dataFlow`, `hasRateLimit`, `t1IntervalMin`
- **DB-stale:** `lastVerified: 2026-04-12` (31 days); `dateFilterEvidence` numbers and `storeApiTotal: 4311` are 31 days stale (live count 4,469, +158).
